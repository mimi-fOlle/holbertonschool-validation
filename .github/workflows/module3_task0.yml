name: module3_task0
on: 
  schedule:
  # Triggers the workflow every day at 5:30 and 17:30 UTC
  # * is a special character in YAML so you have to quote this string
  - cron: '30 5,17 * * *'
[push]
jobs:
  runs-on: ubuntu-latest
  steps:
     # Clone the repo
     - run: git clone https://github.com/mimi-fOlle/holbertonschool-validation.git
     # Execute the command make help
     - run: make help
