---
name: module3_task0
on:
  push:
    branches:
      -main
  schedule:
  # Triggers the workflow every day at 5:30 UTC
  # * is a special character in YAML so you have to quote this string
    - cron: '30 5 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Direct to correct directory
        run: |
          cd module3_task0
          make help
